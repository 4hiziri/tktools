(in-package :cl-user)
(defpackage vigenere-test
  (:use :cl
        :vigenere
        :prove))
(in-package :vigenere-test)

;; NOTE: To run this test file, execute `(asdf:test-system :vigenere)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
