(in-package :cl-user)
(defpackage subst-test
  (:use :cl
        :subst
        :prove))
(in-package :subst-test)

;; NOTE: To run this test file, execute `(asdf:test-system :subst)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
